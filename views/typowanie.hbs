<main>
    <section>
        <div class="container">
            <h2 class="text-center">Typowanie</h2>
            <p>Wybierz kolejkę</p>
            {{#each typy}}
            <ul class="list-group">
                <a href="typowanie/kolejka/{{kolejka @index}}" class="list-group-item d-flex justify-content-between align-items-center">Kolejka {{kolejka @index}}<span class="badge badge-secondary d-inline-block ml-auto">{{typesReady this}}</span></a>
            </ul>
            {{/each}}
        </div>
    </section>
</main>